import heapq
# 우선순위 큐 -> 이진힙 -> heapq 라이브러리 사용
pq = []
heapq.heappush(pq, (0, 0))  # 우선순위에 따라 뽑아올 수 있음
                        # 첫번째 원소는 가장 작은 값을 비교 뽑아오는 것
                        # 두번째 원소는 값





heapq.heappop(pq)